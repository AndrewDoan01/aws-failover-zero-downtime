apiVersion: v1
kind: Service
metadata:
  name: retail-store-sample-app
  labels:
    app.kubernetes.io/name: retail-store-sample-app
spec:
  selector:
    app.kubernetes.io/name: retail-store-sample-app
  ports:
    - name: http
      port: 80
      targetPort: http
