apiVersion: v1
kind: ConfigMap
metadata:
  name: catalog-config
  labels:
    app.kubernetes.io/name: catalog
    app.kubernetes.io/component: backend
data:
  LOG_LEVEL: "info"
  DATABASE_ENABLED: "true"
  CACHE_ENABLED: "true"
  CORS_ENABLED: "true"
  API_TIMEOUT: "30"
